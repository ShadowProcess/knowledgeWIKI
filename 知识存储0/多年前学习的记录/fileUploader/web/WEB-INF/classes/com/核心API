1.DiskFileItemFactory

    作用：可以设置缓存大小以及临时文件的保存位置

    默认缓存大小10k,[10240B]
    临时文件默认存储在当前操作系统的临时文件目录  可以在环境变量中查看

    1.new DiskFileItemFactory();
        缓存大小和临时文件使用默认的
    2.new DiskFileItemFactory(int sizeThreshold,File repository);
        sizeThreshold  缓存大小
        repository  临时文件位置
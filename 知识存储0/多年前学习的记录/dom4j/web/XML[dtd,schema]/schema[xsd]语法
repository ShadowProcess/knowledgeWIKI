XML的编程

* JAXP的SAX解析(了解怎么样解析)

* DOM4j的解析(必须会,企业用)


schema和DTD约束的对比：

    schema的出现是为了克服DTD的局限性 （DTD只能引入一个文件）


    schema符合XML的语法结构
    schema对名称空间支持的好 java.util.list
    schema支持更多的数据类型 自定义的数据类型

XML schema的后缀名为xsd



配置初始化参数：
    需要在<servlet></servlet>标签中配置
        如果配置在了某个servlet中，只能在该servlet获取对应参数



//获取web.xml中配置的server-name：
String getServletName()

//获取初始化参数：
String getInitParameter(String name)

//获取初始化参数们：
Enumeration getInitParameterNames()